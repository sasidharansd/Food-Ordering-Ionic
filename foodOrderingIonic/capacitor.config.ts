import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'foodOrderingIonic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
